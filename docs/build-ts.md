# `build-ts` script

Add TypeScript build and clean scripts.

```shell
ft build-ts
```

## Help

```
ft build-ts [options]

    Add TypeScript build and clean scripts.

Options
    --dir-dist      dist folder, defaults to 'lib'
    -h, --help      show this output
```
